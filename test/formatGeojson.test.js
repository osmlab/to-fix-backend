
var formatGeojson = require('../src/utils/formatGeojson');
var test = require('tape');

var fixtures = {};

test('it converts a GeoJSON file into line-delimited features', function(t) {
  t.fail('just pass');
  t.end();
});
